# Running Project On Local

    - clone project
    - docker compose build
    - docker compose up -d
    - docker exec -it gsmpay_php sh
    - composer install
    - cp .env.example .env
    - php artisan deploy 
    - php artisan test
